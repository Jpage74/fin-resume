# 财经学生简历私人助手 Agent — 设计文档

> 基于 Google ADK（Agent Development Kit）的私人求职助手项目
> **目标用户：财经类院校学生**，聚焦其典型校招就业岗位
> 目标：做成一个可发布到 GitHub 的简历项目，覆盖 **RAG + 记忆层 + 定时数据收集 + 多 Agent 协作 + LiteLLM 统一模型接入 + 防幻觉机制**
> 定位：不仅是"JD 进报告出"的查询工具，更是**能记住用户的私人助理**——跨会话保留画像、跟踪投递进展、每日自动沉淀记忆

---

## 一、项目定位

一个「输入 JD 就能获得完整求职建议」的**私人求职助手**，**服务于财经类院校学生的校招/求职场景**：
它以"私人助理"自居——发一次简历就记住你的背景，跨会话认得你的偏好与进展，越用越懂你：

- 输入一份岗位 JD → 解析需求
- 结合**按财经岗位分类的知识库（RAG）**检索：匹配的岗位 + 上岸者背景画像
- 结合**用户画像记忆**，对简历做**差距分析**（技能覆盖、证据缺口、关键词补全）
- 结合**投递历史记忆**，去重、跟踪、复盘
- 知识库通过**定期联网搜索**自动丰富（按不同财经岗位分别积累）

### 用户范围（刻意收窄）

- **目标用户**：财经类院校学生（金融学、会计学、财政学、经济学、审计学、投资学、国际经济与贸易、精算、税收等专业），以应届校招为主，兼顾实习。
- **聚焦价值**：垂直场景比通用"岗位匹配"更有护城河——目标岗位固定、数据好搜集、案例质量高、作者本人即用户（真实性+可日常自用）。
- **明确不做**：不做全行业全岗位泛化搜索，不做自动投递，不批量爬取招聘平台。

---

## 二、目标用户岗位画像（财经类院校典型就业方向）

知识库按以下**岗位大类 + 细分方向**组织（`role` 字段），种子数据先覆盖最核心的几类：

| 大类 | 典型岗位 | 代表雇主 / 去向 |
|---|---|---|
| 券商 / 投行 | 投行部(IBD)、研究所(行研)、财富管理、营业部 | 中金、中信、华泰、国泰君安等 |
| 银行 | 总行/分行管培生、客户经理、风控、金融市场部 | 四大行、股份制、城商行 |
| 会计师事务所 | 审计、税务、财务咨询 | 四大（普华/德勤/安永/毕马威）、内资八大 |
| 基金 / 资管 | 研究员、交易员、产品岗 | 公募、私募 |
| 保险 | 精算、核保理赔、运营 | 人保、平安、太保 |
| 咨询 | 战略咨询、财务咨询、商业分析 | MBB、四大咨询、内资咨询 |
| 互联网 / 科技 | 财务BP、商业分析、数据分析、运营 | 大厂财务/商分/数分岗 |
| 国企 / 央企 | 财务部、资金管理、审计部 | 央企总部及子公司财务岗 |
| 考公 / 事业单位 | 税务局、财政局、人民银行、金融监管 | 财经类对口招录岗位 |

> MVP 建议先选 2~3 类种子做深（如：券商行研、银行管培、四大审计），跑通后再扩展。

---

## 三、核心设计原则

1. **策展人（Curator）而不是爬虫**：自动收集只负责"抓取 → 质量过滤 → 结构化提取 → 去重 → 入库"，不做大规模硬爬。
2. **公开数据源即可**：不需要 HR 内部资料（拿不到也不需要）。用公开 JD、公开简历案例、公开薪资/趋势报告。
3. **种子库保质量，自动库保规模**：MVP 用手工精选种子数据保证质量；自动收集进 `candidates` 表，人工/规则审核后才进正式库。
4. **记忆优先**：用户画像 + 投递历史是核心资产，一次录入、多次复用，杜绝冷启动。
5. **证据驱动、防幻觉**：所有结论必须可溯源，无证据内容标记 `needs_proof`，宁可低置信度拒答也不编造。
6. **统一模型接入（LiteLLM）**：所有 LLM 调用走 LiteLLM 网关，便于切换/回退模型、统一日志与成本。
7. **数据合法性**：避开 ToS 禁止抓取的平台，优先官方校招页、求职社区公开内容、开放数据集。
8. **定时任务放 GitHub Actions**：ADK 不负责调度，用 GitHub Actions cron 每日触发收集器，日志和增量 commit 即 CI/CD 展示点。

---

## 四、整体架构

**核心流程：输入 JD + 简历 → ① JD 解析 → ② 差异分析 & ③ RAG 案例检索（并行）→ ④ 汇总报告 → ⑤ 用户确认一键修改。**

```
 ┌──────────────── 输入 ────────────────┐
 │  ① 岗位 JD（或只给岗位名）            │
 │  ② 我的简历（PDF / DOCX / 文本）      │
 └──────────────────┬──────────────────┘
                    ▼
        ┌──────────────────────┐
        │  ① JD 解析            │  硬门槛(学历/证书/六级/年限)
        │  提取 硬性/软性/加分项  │  用规则校验，不靠 LLM 猜
        └─────────┬────────────┘
                  ▼
     结构化岗位要求 JobRequirements
                  │
       ┌──────────┴──────────┐
       ▼                     ▼
┌────────────────┐  ┌──────────────────────┐
│ ② 差异分析      │  │ ③ RAG 检索优秀案例     │
│ 简历 vs 岗位要求 │  │ 按岗位/要求从知识库     │
│ →技能/经历缺口  │  │ 检索上岸者背景画像        │
│ →needs_proof   │  │ →匹配岗位+案例(带来源) │
└───────┬────────┘  └──────────┬───────────┘
        └──────────┬───────────┘
                   ▼
      ┌──────────────────────────┐
      │  ④ 汇总报告 + 修改意见     │  ← reviewer 复核(防幻觉)
      │  硬门槛结果 / 差距 /       │     无来源断言 → 打回
      │  参考案例 / 逐条修改建议    │
      └────────────┬─────────────┘
                   ▼
      ┌──────────────────────────┐
      │  ⑤ 用户确认 → 一键修改     │
      │  逐条确认(needs_proof     │  → 应用 → 更新简历
      │  须补证据才允许写入)       │  → 写回记忆层画像
      └──────────────────────────┘
```

**说明**

- **双输入源**：有具体 JD → 走 ①JD 解析；只有岗位名（如"券商-行研"）→ `role_profiler` 从知识库 + web 搜索聚合岗位典型要求，同样产出 `JobRequirements`。
- **② ③ 并行**：在 ① 之后同时进行，都消费 `JobRequirements`，互不依赖。
- **记忆层贯穿全程**：②差异分析读用户画像；⑤一键修改后写回画像与投递历史。
- **reviewer 复核**：在 ④ 报告产出前核对事实，防幻觉。
- **LiteLLM 统一网关**：所有 LLM 调用走它，低温 + 固定 seed。
- **定时收集器**（GitHub Actions cron）独立于对话循环，负责知识库自动增长。

### 组件说明

| 组件 | 职责 | 输入 → 输出 | ADK 能力点 |
|---|---|---|---|
| jd_analyzer | 解析 JD，提取硬性/软性/加分项（规则校验硬门槛） | JD 文本 → `JobRequirements` | 子 agent + schema |
| role_profiler | 只有岗位名时，聚合岗位典型要求（可选路径） | 岗位名 → `JobRequirements` | 子 agent + 工具 |
| resume_matcher | 简历 + 画像对比要求，逐条带证据，无证据标 `needs_proof` | `JobRequirements` + 画像 → 差距报告 | 子 agent + 防幻觉 |
| case_retriever | 按 `role` 粗筛 + 要求语义精排，返回匹配岗位 + 案例 | `JobRequirements` → 案例列表 | 子 agent + RAG |
| reviewer | 报告事实复核：无来源断言、疑似编造 → 打回 | 报告 → 通过 / 打回 | 子 agent + 核查循环 |
| apply_revision | 一键修改：逐条确认后应用，写回简历与记忆层 | 修改建议 + 用户确认 → 新简历 | 工具 + 用户确认 |
| 主 Agent 求职助手 | 编排管线、注入记忆、汇总 | 用户请求 → 最终报告 | 主 agent + session/记忆 |

> **RAG 检索不受流程影响**：向量库（Chroma）、embedding、top_k、相似度阈值、hybrid+rerank、防幻觉机制全部照旧。唯一变化是检索 query 从原始 JD 文本换成结构化 `JobRequirements` + role 粗筛——输入更干净，命中更准。

### 分层架构（纵向视角）

```
┌────────────────────────────────────────────────────────────┐
│ ① 接入层 Interface                                          │
│   CLI · Web UI (adk web) · (可扩展 REST API)               │
└───────────────────────────┬────────────────────────────────┘
                            ▼
┌────────────────────────────────────────────────────────────┐
│ ② 编排层 Orchestration                                     │
│   主 Agent：任务理解 · 流程控制 · 记忆注入 · 结果汇总          │
│   子 agent 调度（何时调谁、参数传递、异常/重试处理）            │
└───────────────────────────┬────────────────────────────────┘
                            ▼
┌────────────────────────────────────────────────────────────┐
│ ③ 执行层 Execution                                         │
│   子 Agent：jd_analyzer · role_profiler · resume_matcher   │
│             case_retriever · reviewer · apply_revision     │
│   工具：web_search · kb_manage · 文档解析(PDF/DOCX/TXT)     │
└───────────────────────────┬────────────────────────────────┘
                            ▼
┌────────────────────────────────────────────────────────────┐
│ ④ 模型层 Model（LiteLLM 统一网关）                           │
│   deepseek-v4-flash(开发默认) · deepseek-v4-pro(重活/复核)    │
│   低温(0.1) + 固定 seed · 失败自动 fallback · 统一日志/成本    │
└───────────────────────────┬────────────────────────────────┘
                            ▼
┌────────────────────────────────────────────────────────────┐
│ ⑤ 数据层 Data                                              │
│   知识库：Chroma 向量库（按财经岗位分 collection）            │
│           + 种子/审核数据(candidates→approved)              │
│   记忆层：用户画像 profile.yaml · 投递历史 SQLite             │
│   会话：ADK Session / Memory API                            │
└───────────────────────────┬────────────────────────────────┘
                            ▼
┌────────────────────────────────────────────────────────────┐
│ ⑥ 自动化/基建层 Infra                                       │
│   GitHub Actions cron（定时收集器）· 日志 · RAG评测(recall@k) │
└────────────────────────────────────────────────────────────┘
```

- **调用方向自上而下**：接入层 → 编排层 → 执行层 → 模型层 → 数据层；基建层横切支撑（定时任务、日志、评测）。
- **与流程图的对应**：流程图是"一次用户请求的时序"，分层图是"系统的静态结构"，两者是同一套组件从两个视角看。
- **可替换性**：模型层只认 LiteLLM 网关，换模型/换 provider 不动业务代码；数据层独立，换向量库/存储不影响上层。

---

## 五、记忆层设计

三层记忆**严格分离存储**，职责不同：

### 1. 用户画像记忆（User Memory）— `profile.yaml` / JSONL
一次录入、跨会话复用，会话启动时注入主 agent 上下文：

```yaml
user:
  name: ""                    # 本地存储, gitignore
  school: ""                  # 财经类院校
  major: ""                   # 金融/会计/...
  degree: ""                  # 本科/硕士
  gpa: ""
  target_roles: ["券商-行研", "银行-管培"]   # 目标岗位
  target_city: []
  constraints: []             # 实习时长、可接受薪资等
  skills:
    - name: "Python"
      level: "中"
      evidence: "..."         # 证据: 项目/课程/证书
    - name: "SQL"
      level: "中"
      evidence: null          # 无证据 → needs_proof
  certs: []                   # CPA/CFA/英语六级等
  projects: []                # 项目经历(含量化结果)
```

### 2. 投递历史记忆（Application History）— SQLite
`applications` 表，供去重、跟踪、复盘：

| 字段 | 说明 |
|---|---|
| jd_id / source | JD 来源 |
| company / role | 公司 + 岗位 |
| status | seen / analyzed / applied / rejected / offer |
| match_score | 匹配分（含证据） |
| needs_proof_items | 缺证据项（待补） |
| applied_at | 时间 |

**与定时收集联动**：collector 新增 JD 前先查 `applications` 去重，避免反复推荐已看过的岗位。

### 3. 会话状态（Session State）— ADK Session/Memory API
- 短期：当前对话上下文、本次 JD 的分析过程。
- 长期：ADK 记忆 API（`UserMemoryService` / 长时记忆）承载用户画像的读写与检索。

### 记忆层关键设计

- **隐私**：用户画像含真实个人信息 → 默认 `.gitignore` 排除、本地存储、不上传；README 明示隐私边界。
- **更新机制**：简历更新、投递新岗位时同步写回；`needs_proof` 项补齐证据后状态升级。
- **注入时机**：每次会话启动读取画像注入主 agent；投递历史按需检索（如"我投过哪些行研岗"）。

### 4. 每日摘要自动化（待实现）

长期记忆（LTM）的每日摘要是手动触发的（`scripts/run_memory_digest.py`），目标是从手动升级为**自动**。

**自动化方式（两条互补）：**

- **方式 A — 内嵌 web 服务**：`run_web.py` 启动时起一个后台定时线程，每天固定时间（如 23:00）自动调用 LTM 摘要并写回三 MD。
  依赖：web 服务**常驻运行**。服务开着 → 摘要自动发生，用户零操作。
- **方式 B — Windows 任务计划**：在 Windows 任务计划程序里注册每天定时任务，执行 `python scripts/run_memory_digest.py`。
  依赖：电脑开机、任务已注册。服务不常驻也能跑，最稳。

**方案 C（GitHub Actions cron）已排除**：session.db 本地存储、不上传，CI 拿不到对话数据，无法做摘要。

**实现要点（方式 A）：**
- 后台线程 `while True`：计算到下次触发时间的秒数 → sleep → 调 `ltm.digest_day()` → 更新下次触发时间。
- 与 web 服务生命周期绑定：服务退出即停；每天只跑一次（记录上次摘要日期，避免重复）。
- 摘要占用一次 LLM 调用（DeepSeek flash，成本可忽略），失败不阻塞服务。

---

## 六、LLM 接入与防幻觉机制

### 1. LiteLLM 统一模型网关

- 所有 LLM 调用（主 agent、子 agent、collector 分析）统一走 LiteLLM，`config.yaml` 声明模型清单。
- 支持：GLM/BigModel、DeepSeek、OpenAI 兼容、Gemini 等，可一键切换，模型失败自动 fallback。
- **生成阶段统一控制**：低温度（如 0~0.3）、top_p 偏低（如 0.9）、固定 seed → 输出稳定、可复现、不飘。
- ⚠️ **DeepSeek 推理模型必须禁 thinking**：deepseek-v4-flash/pro 默认输出 `reasoning_content`（思考过程），而 ADK 2.6 的 LiteLlm 会**误把思考内容当回复**（表现为英文/复述指令）。所有调用必须带 `extra_body={"thinking": {"type": "disabled"}}`，否则回复是思考过程而非答案。这是已实测踩过的坑。
- ⚠️ **temperature/top_p 只影响生成采样，不影响检索**——"不给出不相关案例"由检索侧参数保证（top_k 小、相似度阈值、hybrid+rerank，见下方防幻觉机制），低温只是让生成不跑偏；低温 + 坏检索 = 自信地输出错误内容，必须配 grounding 约束。

### 2. 防幻觉机制（证据驱动）

| 机制 | 做法 | 对应组件 |
|---|---|---|
| **证据绑定** | 每个匹配分、每个差距点必须附证据引用（简历条目 / JD 条目 / 案例 source）；无证据不写 | resume_matcher |
| **needs_proof 三态** | 技能/经历分三态：有证据 / 需补证据（`needs_proof`）/ 无证据不可写；写入简历建议前必须用户确认 | resume_matcher + 确认机制 |
| **硬门槛规则校验** | 学历、CPA/CFA、英语六级、实习年限等硬性要求用**确定性规则**先过一遍，不靠 LLM 自由判断 | jd_analyzer |
| **检索约束 + 检索参数** | 只基于检索到的文档作答；检索为空时明说"库中无匹配案例"，不编造。**检索侧参数保证相关性**：top_k 小（3~5）、相似度阈值截断、hybrid 检索 + rerank | case_retriever |
| **结构化输出 + Schema 校验** | Pydantic schema 约束输出，校验失败自动重试 | 全部子 agent |
| **reviewer 复核** | 最终报告由 reviewer 子 agent 复核：无来源断言、疑似编造、自相矛盾 | reviewer |
| **低置信度拒答** | 不确定内容标注 `low-confidence` 或明确"无法确认"，不硬答 | 全部 agent |
| **RAG 质量评测** | recall@k、证据覆盖率、幻觉率持续监控 | 工程化收尾 |

---

## 七、数据来源与策略（财经向）

### 可用的公开数据源

| 类型 | 来源 | 说明 |
|---|---|---|
| 校招 JD | 各券商/银行/四大/央企**官方校招页**、校招公众号、应届生求职网、牛客网 | 优先官方与开放源 |
| 简历案例 | 应届生论坛晒offer、知乎财经求职话题、牛客网、GitHub 开源简历（金融向） | 人工筛选过的公开内容 |
| 招聘趋势 | 人行/金融监管招录公告、CPA/CFA 报考要求、公开薪资报告 | 辅助信息 |
| 笔试面试面经 | 牛客网题库、应届生论坛面经帖 | 可作增强内容 |

### 入库格式（记录 schema 草案）

```
{
  "id": "uuid",
  "type": "jd | resume_case | trend | interview_note",
  "role": "券商-行研",              # 财经岗位类别（KB 按 role 分 collection）
  "source": "url / 来源",
  "title": "标题 / 岗位名",
  "content": "正文 / 摘要",
  "skills": ["...", "..."],
  "quality_score": 0.0-1.0,       # 质量评分（人工/规则）
  "status": "seed | candidate | approved",   # 种子 / 待审核 / 已入库
  "verified": false,               # 是否经人工/规则核验
  "collected_at": "timestamp"
}
```

---

## 八、落地路径（分期）

### MVP（先跑通，最小可演示口径）

**收敛原则**：种子量砍到最小可演示，先把核心闭环跑通，再扩展。

1. **种子知识库（最小量）**：先做 2 个岗位——券商行研 + 银行管培，各 15~20 条简历案例 + 50~100 条 JD。⚠️ 数据收集是最大时间黑洞，勿用 LLM 编造案例充数（项目卖点是证据驱动）。
2. **极简记忆层**：`profile.yaml` 用户画像 + SQLite 投递历史表（先不上 ADK 完整记忆 API）。
3. **核心闭环**：输入 JD → `jd_analyzer` 抽需求（硬门槛规则校验）→ `case_retriever` 检索匹配岗位 + 上岸者背景画像 → `resume_matcher` 证据化差距分析（needs_proof）。
4. **优先级反转**：防幻觉机制（reviewer + 证据绑定）与 RAG 评测（recall@k）**提前做**，这是简历上最值钱的部分；定时收集放最后（最容易无限膨胀）。
5. **LiteLLM 接入 GLM（BigModel）**，CLI 可跑通，`adk web` 可视化。
6. **验收标准（demo 能演示什么）**：输入一份真实 JD → 输出 ① 硬门槛校验结果 ② 匹配岗位 + 案例（带 source）③ 证据化差距分析（含 needs_proof 项）④ reviewer 复核通过。

**MVP 时间估计**（每天 1~2 小时兼职）：技术实现 2~3 周 + 种子数据 1~2 周 ≈ **3~4 周**；建议先交付可 demo 的 MVP 发 GitHub 占坑，README 写清 Roadmap。

### 增强 1：联网搜索
- 接入 web_search 工具（Tavily / Serper / Google CSE），对话中实时补充搜索。

### 增强 2：定时自动收集（**优先级最低，MVP 之后再做**）
- GitHub Actions cron 每日触发 collector → 自动抓取 → 过滤 → 入库（查重投递历史）。
- 自动抓取的进 `candidates` 表，审核通过后才进正式库。

### 增强 3：工程化收尾
- reviewer 复核环节 + 防幻觉完整落地；`adk web` UI + README 架构图 + demo 截图。
- RAG 检索质量评估脚本（recall@k、证据覆盖率、幻觉率），量化展示。

---

## 九、简历价值（面试讲点）

- **垂直场景差异化**：聚焦财经类院校就业，垂直知识库比通用"岗位匹配"更有护城河，且作者本人即目标用户。
- **RAG + 记忆层**：按财经岗位分 collection 的向量检索 + 用户画像/投递历史记忆，检索与记忆质量均可量化。
- **防幻觉机制**：证据绑定、needs_proof、硬门槛规则校验、reviewer 复核——这是 AI 工程面试最加分的设计点。
- **定时数据管道**：GitHub Actions cron + 增量更新，CI/CD 一体。
- **多 Agent 协作**：JD 解析 / 匹配 / 检索 / 复核多个子 agent 分工，体现 ADK 编排能力。
- **统一模型接入**：LiteLLM 网关，多模型可切换、可回退，体现工程完备性。
- **真实可用**：作者本人求职季使用，README 放真实跑通 demo。

---

## 十、风险与注意

- **数据质量是最大成本**：财经岗位的高质量简历案例集中在求职社区/公众号，可自动抓的有限 → 用种子库保底 + 人工策展。
- **合法性**：不硬爬 BOSS直聘 / LinkedIn 等 ToS 禁止的平台；校招信息优先走官方校招页。
- **隐私**：用户画像含真实个人信息，默认 `.gitignore` 排除、本地存储。
- **防幻觉是持续工程**：防幻觉机制要配 RAG 评测一起落地，否则难以证明有效。
- **范围控制**：全量功能很大，务必按 MVP → 增强分阶段推进，岗位范围先 2~3 类做深再扩展。
