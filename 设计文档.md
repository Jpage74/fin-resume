# 财经学生简历私人助手 Agent — 设计文档

> 基于 **Google ADK 2.6 + LiteLLM** 的私人求职助手项目
> **目标用户：财经类院校学生**，聚焦其典型校招就业岗位
> 目标：做成一个可发布到 GitHub 的简历项目，覆盖 **RAG + 记忆层 + 定时数据收集 + 多 Agent 协作 + LiteLLM 统一模型接入 + 防幻觉机制**
> 定位：不仅是"JD 进报告出"的查询工具，更是**能记住用户的私人助理**——跨会话保留画像、跟踪投递进展、每日自动沉淀记忆
>
> **状态图例**：`【已实现】` = MVP 已落地；`【规划中】` = 设计完成、待实现。

---

## 一、项目定位

一个「输入 JD + 简历就能获得完整求职建议」的**私人求职助手**，**服务于财经类院校学生的校招/求职场景**：
它以"私人助理"自居——发一次简历就记住你的背景，跨会话认得你的偏好与进展，越用越懂你：

- 输入一份岗位 JD → 解析需求
- 结合**按财经岗位分类的知识库（RAG）**检索：匹配的岗位 + 上岸者背景画像
- 结合**用户画像记忆**，对简历做**差距分析**（技能覆盖、证据缺口、关键词补全）
- 结合**投递历史记忆**，去重、跟踪、复盘
- 长期记忆每日摘要自动沉淀到 `data/memory/` 三类 MD（用户画像 / 助手设定 / DREAM 长期经历）`【已实现，自动化部分规划中】`

### 用户范围（刻意收窄）

- **目标用户**：财经类院校学生（金融学、会计学、财政学、经济学、审计学、投资学、国际经济与贸易、精算、税收等专业），以应届校招为主，兼顾实习。
- **聚焦价值**：垂直场景比通用"岗位匹配"更有护城河——目标岗位固定、数据好搜集、案例质量高、作者本人即用户（真实性+可日常自用）。
- **明确不做**：不做全行业全岗位泛化搜索，不做自动投递，不批量爬取招聘平台。

---

## 二、目标用户岗位画像（财经类院校典型就业方向）

知识库按以下 **role_category 岗位大类**组织（`jds` 与 `cases` 两个向量集合共用这套词表），
当前 `schemas.py` 已支持的完整词表（15 类）：

| 大类 | 典型岗位 | 代表雇主 / 去向 |
|---|---|---|
| 券商 / 投行 | 投行部(IBD)、研究所(行研)、债券承做、战略客户 | 中金、中信、华泰、国泰君安、国信 |
| 券商行研 | 行业研究（卖方）、投资研究助理 | 兴业、Top2 券商研究所等 |
| 银行 | 总行/分行管培生、客户经理、风控、金融市场部 | 四大行、股份制、城商行 |
| 会计师事务所 | 审计、税务、财务咨询 | 四大（普华/德勤/安永/毕马威）、内资八大 |
| 基金 / 资管 | 基金投研、基金销售（渠道/直销）、基金运营、投顾助理 | 汇添富、嘉实、泰康、国金资管 |
| 保险 | 保险资管（另类投资等）、精算、核保理赔、运营 | 人保资本、中华保险、平安、太保 |
| 信托 | 信托业务（固收、非标等） | 央企信托 |
| 咨询 | 战略咨询、ESG 咨询、商业分析 | MBB、四大咨询、内资咨询 |
| 互联网 / 科技 | 财务BP、商业分析、数据分析、运营 | 大厂财务/商分/数分岗 |
| 国企 / 央企 | 财务部、资金管理、审计部 | 央企总部及子公司财务岗 |
| 考公 / 事业单位 | 税务局、财政局、人民银行、金融监管 | 财经类对口招录岗位 |
| 私募 / 股权投资 | PE / VC 投资分析 | 人保资本等 PE |

> **种子数据现状**：`data/knowledge/jds/` 已入库 **16 条 JD**（覆盖 10 类：券商投行 4、券商行研 3、基金投研 2，PE/保险资管/信托/基金销售/基金运营/咨询/互联网财务各 1）；
> `data/knowledge/cases/` 已入库 **3 条上岸者背景画像**（券商投行 / 券商行研 2 类）。
> bg 案例库是当前短板，按"宁缺毋滥、防幻觉"原则优先保证质量，持续扩充中。

---

## 三、核心设计原则

1. **策展人（Curator）而不是爬虫**：自动收集只负责"抓取 → 质量过滤 → 结构化提取 → 去重 → 入库"，不做大规模硬爬。`【MVP：手工策展；自动收集规划中】`
2. **公开数据源即可**：不需要 HR 内部资料（拿不到也不需要）。用公开 JD、公开简历案例、公开薪资/趋势报告。
3. **种子库保质量，自动库保规模**：MVP 用手工精选种子数据（`data/knowledge/{jds,cases}/` 的 yaml）保证质量；`【规划中】`自动收集进 `candidates` 表，人工/规则审核后才进正式库。
4. **记忆优先**：用户画像 + 投递历史 + STM/LTM 是核心资产，一次录入、多次复用，杜绝冷启动。
5. **证据驱动、防幻觉**：所有结论必须可溯源，无证据内容标记 `needs_proof`，宁可低置信度拒答也不编造。
6. **统一模型接入（LiteLLM）**：所有 LLM 调用走 LiteLLM 网关，便于切换/回退模型、统一日志与成本。
7. **数据合法性**：避开 ToS 禁止抓取的平台，优先官方校招页、求职社区公开内容、开放数据集。
8. **定时任务放 GitHub Actions**：`【规划中】`ADK 不负责调度，用 GitHub Actions cron 每日触发收集器，日志和增量 commit 即 CI/CD 展示点。

---

## 四、整体架构

**核心流程：输入 JD + 简历 → ① JD 解析 → ② 硬门槛校验 & ③ RAG 案例检索（并行）→ ④ 证据化差距分析 → ⑤ reviewer 复核。**

```
 ┌──────────────── 输入 ────────────────┐
 │  ① 岗位 JD（原文/文本粘贴）            │
 │  ② 我的简历（PDF / DOCX / 文本）      │
 └──────────────────┬──────────────────┘
                    ▼
        ┌──────────────────────┐
        │  ① jd_analyzer        │  硬门槛(学历/证书/六级/实习)
        │  JD → JobRequirements │  规则校验，不靠 LLM 猜
        └─────────┬────────────┘
                  ▼
     结构化岗位要求 JobRequirements
                  │
       ┌──────────┴──────────┐
       ▼                     ▼
┌────────────────┐  ┌──────────────────────┐
│ ② 硬门槛校验    │  │ ③ case_retriever      │
│ 画像 × evidence │  │ RAG：jds 岗位库 +      │
│ → PASS/FAIL/   │  │      cases 背景画像库   │
│   UNKNOWN      │  │ → 匹配岗位+案例(带source│
└───────┬────────┘  └──────────┬───────────┘
        └──────────┬───────────┘
                   ▼
      ┌──────────────────────────┐
      │  ④ resume_matcher         │  证据化差距分析
      │  MatchReport(0~100)       │  needs_proof 标记
      │  verdict/差距/亮点/建议    │
      └────────────┬─────────────┘
                   ▼
      ┌──────────────────────────┐
      │  ⑤ reviewer 复核          │  防幻觉硬门：
      │  approved / 打回修正      │  verdict 一致性 /
      │                           │  source 溯源 / 证据绑定
      └────────────┬─────────────┘
                   ▼
      ┌──────────────────────────┐
      │  输出：结构化分析报告       │
      │  ①硬门槛 ②匹配岗位&案例    │
      │  ③差距 ④复核 ⑤修改建议     │
      └──────────────────────────┘
```

**说明**

- **双输入源**：`【已实现】`目前只支持「直接给 JD 原文」（CLI `--file`/文本、web 粘贴）；
  `【规划中】`只给岗位名（如"券商-行研"）→ `role_profiler` 从知识库 + web 搜索聚合岗位典型要求，同样产出 `JobRequirements`。
- **② ③ 并行**：都在 ① 之后进行，都消费 `JobRequirements`，互不依赖。
- **记忆层贯穿全程**：②硬门槛校验读 `profile.yaml` 画像；每次调 LLM 前 `before_model_callback` 注入 STM/LTM 记忆。
- **⑤ reviewer 复核**：在报告产出前核对事实，防幻觉（error 级 finding → approved=False）。
- **LiteLLM 统一网关**：所有 LLM 调用走它，低温 + 固定 seed。
- **定时收集器**（GitHub Actions cron）`【规划中】`，独立于对话循环，负责知识库自动增长。

### 组件清单（MVP 现状）

| 组件 | 状态 | 职责 | 输入 → 输出 | 实现位置 |
|---|---|---|---|---|
| jd_analyzer | `【已实现】` | 解析 JD，提取 hard/soft/bonus 需求（受控 evidence_key 词表） | JD 文本 → `JobRequirements` | `analyzer/jd_analyzer.py` |
| 硬门槛校验 rules | `【已实现】` | 把每条硬门槛的 evidence_key 映射到画像证据，确定性判定 | `JobRequirements` × 画像 → `[GateResult]` | `analyzer/rules.py` |
| case_retriever | `【已实现】` | Chroma 两集合（jds/cases）向量检索，阈值把关 | `JobRequirements` → `RetrievalResult` | `retriever/case_retriever.py` |
| resume_matcher | `【已实现】` | 证据化差距分析（needs_proof 三态、匹配分、verdict） | 需求+画像+检索 → `MatchReport` | `matcher/resume_matcher.py` |
| reviewer | `【已实现】` | 复核：verdict 一致性 / source 溯源 / 证据绑定 | `MatchReport` + 事实底座 → `ReviewResult` | `reviewer/reviewer.py` |
| pipeline 编排 | `【已实现】` | 把四环串成 `run_pipeline()`，CLI/web/agent 共用 | JD+画像 → `PipelineResult` | `pipeline.py` |
| report 格式化 | `【已实现】` | `PipelineResult` → 可读 markdown 报告（五段式） | 结果 → 文本 | `report.py` |
| set_resume 工具 | `【已实现】` | 简历文本 → 画像（LLM 抽取 + 合并 profile.yaml） | 简历 → 画像确认 | `agents/fin_resume/tools.py` + `resume/parser.py` |
| analyze_jd 工具 | `【已实现】` | web 侧一键四环管线 + 报告 | JD → 报告文本 | `agents/fin_resume/tools.py` |
| 主 Agent fin_resume | `【已实现】` | 工具调度 + 开场白 + 记忆注入 + 文件解码 | 用户请求 → 最终回答 | `agents/fin_resume/agent.py` |
| role_profiler | `【规划中】` | 只有岗位名时，聚合岗位典型要求 | 岗位名 → `JobRequirements` | — |
| apply_revision | `【已实现】` | 一键修改：基于最近分析建议改写简历，备份旧版，写回画像与简历文本 | 修改建议 + 简历 → 新简历 + diff | `revision/apply_revision.py` |
| 知识库管理 kb_manage | `【规划中】` | 知识库增删改（后续 web 收集/审核入口） | — | — |

> **检索不受流程影响**：向量库（Chroma）、embedding（fastembed/bge-small-zh）、top_k、相似度阈值、防幻觉机制全部照旧。
> 检索 query 由 `_build_query()` 把 `JobRequirements`（role_category + role_name + company + 各需求描述）拼成文本输入，比原始 JD 更干净、命中更准。

### 分层架构（纵向视角）

```
┌────────────────────────────────────────────────────────────┐
│ ① 接入层 Interface   【已实现】                              │
│   CLI (python -m job_assistant.cli)                        │
│   Web UI (run_web.py → ADK dev-ui，新会话自动开场白)          │
│   文件上传（PDF/Word/文本，before_model_callback 解码）        │
└───────────────────────────┬────────────────────────────────┘
                            ▼
┌────────────────────────────────────────────────────────────┐
│ ② 编排层 Orchestration   【已实现】                          │
│   主 Agent（单 Agent + 2 工具）：任务理解 · 流程控制          │
│   记忆注入 · 结果汇总（非多子 agent，四环为函数化管线）         │
└───────────────────────────┬────────────────────────────────┘
                            ▼
┌────────────────────────────────────────────────────────────┐
│ ③ 执行层 Execution   【已实现】                              │
│   jd_analyzer · rules · case_retriever · resume_matcher    │
│   reviewer · report · resume/parser（简历→画像）            │
│   search/web_search（联网）· revision/apply_revision（一键改简历）│
│   【规划中】kb_manage                                       │
└───────────────────────────┬────────────────────────────────┘
                            ▼
┌────────────────────────────────────────────────────────────┐
│ ④ 模型层 Model（LiteLLM 统一网关）  【已实现】                │
│   deepseek/deepseek-v4-flash（开发默认，全链路使用）          │
│   config/litellm.yaml 声明 flash + pro（pro 暂未启用）       │
│   低温(0.1) + 低 top_p(0.9) + 固定 seed(42)                 │
│   统一 extra_body={"thinking":{"type":"disabled"}}          │
└───────────────────────────┬────────────────────────────────┘
                            ▼
┌────────────────────────────────────────────────────────────┐
│ ⑤ 数据层 Data   【已实现】                                   │
│   知识库：data/knowledge/{jds,cases}/**/*.yaml（手工精选）    │
│   向量库：Chroma 两集合（jds / cases，cosine）               │
│   记忆层：profile.yaml · history.db · data/memory/{STM,LTM} │
│   会话：ADK session.db（agents/fin_resume/.adk/session.db）  │
└───────────────────────────┬────────────────────────────────┘
                            ▼
┌────────────────────────────────────────────────────────────┐
│ ⑥ 自动化/基建层 Infra                                       │
│   RAG 评测 scripts/eval_rag.py（recall@k）【已实现】          │
│   冒烟脚本 scripts/smoke_*.py【已实现】                      │
│   每日摘要 scripts/run_memory_digest.py【已实现·手动】        │
│   GitHub Actions cron 定时收集【规划中】                     │
└────────────────────────────────────────────────────────────┘
```

- **调用方向自上而下**：接入层 → 编排层 → 执行层 → 模型层 → 数据层；基建层横切支撑（定时任务、日志、评测）。
- **与流程图的对应**：流程图是"一次用户请求的时序"，分层图是"系统的静态结构"，两者是同一套组件从两个视角看。
- **可替换性**：模型层只认 LiteLLM 网关，换模型/换 provider 不动业务代码；数据层独立，换向量库/存储不影响上层。

---

## 五、记忆层设计

**四层记忆严格分离存储，职责不同**（MVP 实现与原"三层"设计相比，把"会话状态"细化为 STM + LTM 两层）：

### 1. 用户画像记忆（User Memory）— `profile.yaml`  `【已实现】`
一次录入、跨会话复用，硬门槛校验时读它取证据：

```yaml
user:
  name: "张三"                # 本地存储, gitignore
  school: "上海财经大学"       # 财经类院校
  major: "金融学"
  degree: "硕士"              # 本科/硕士/博士
  gpa: "3.6/4.0"
  target_roles: ["实习"]      # 目标岗位
  target_city: ["北京"]
  constraints: ["在校-考虑机会"]
  skills:                     # 技能（含证据）
    - name: "Python"
      level: "熟练"
      evidence: ""            # 证明来源：课程/项目/证书
  certs: ["证券从业资格", "CET-6"]
  internships: ["华泰证券研究所-行研实习生（6个月，...）"]  # 实习经历
  projects:                   # 项目经历
    - name: "新能源行业盈利预测模型"
      detail: "覆盖20家标的"
```

- 更新：web 发简历 → `set_resume` → `resume/parser.parse_resume()`（LLM 抽取）→ 合并写回。
- 无证据字段留空/空列表，绝不编造。

### 2. 投递历史记忆（Application History）— `history.db`（SQLite） `【已实现】`
`applications` 表，供去重、跟踪、复盘：

| 字段 | 说明 |
|---|---|
| jd_id | JD 标识（source 或 role_name） |
| company / role | 公司 + 岗位 |
| status | seen / analyzed / applied / rejected / offer |
| match_score | 匹配分 |
| needs_proof_items | 缺证据项（JSON 数组） |
| applied_at | 时间 |

- 目前 CLI 分析完成后自动写入一条（`status=analyzed`）。
- `【规划中】`定时收集器新增 JD 前先查 `applications` 去重，避免反复推荐已看过的岗位。

### 3. 短期记忆（STM）— 极简版  `【已实现】`
- **不做 LLM 压缩**（MVP 决策）：只保留**最近 20 分钟内**所有会话的对话原文（最多 10 条），供相邻会话衔接。
- 数据源：ADK 自动生成的 `session.db`（events 表，author 在 event_data JSON 内），工具事件过滤、只保留人话。
- 输出形如 `[系统短期记忆]（最近 20 分钟内，共 N 条）`，落盘 `data/memory/stm_ctx.json`（排查用）。

### 4. 长期记忆（LTM）— 每日摘要 → 三类 MD  `【已实现·手动触发】`
- 每天对当天对话做一次 LLM 摘要，按 3 个维度拆开，`## YYYY-MM-DD` 节 **APPEND**（不覆盖历史）：

| 维度 | 落盘文件 | 内容 |
|---|---|---|
| user_profile_updates | `user_profile.md` | 画像类（能力/目标/短板/进展） |
| agent_setting_updates | `agent_setting.md` | 行为偏好类（风格/强调/忽略） |
| dream_updates | `dream.md` | 经历类（发生了什么/决策/发现） |

- 触发：`python scripts/run_memory_digest.py [--day YYYY-MM-DD] [--dry-run] [--history]`。
- 敏感个人信息（姓名、联系方式）摘要时被过滤，写"用户"。

### 5. 注入机制  `【已实现】`
- **方式**：ADK `before_model_callback=[_decode_uploaded_files, inject_memory]`——**每次调 LLM 前**，`inject_memory` 实时从磁盘读 STM + LTM，`append_instructions()` 追加到 system_instruction。
- 因此无需重启服务：新的每日摘要 / 短期上下文会被下一次调用自动吃到；注入失败不阻塞主流程（记忆是辅助）。
- agent INSTRUCTION 里配了【系统记忆使用规则】：记忆只用于理解语境与个性化回答，不逐字复述；与本次消息冲突以本次为准。

### 6. 每日摘要自动化（待实现）

长期记忆（LTM）的每日摘要是手动触发的（`scripts/run_memory_digest.py`），目标是从手动升级为**自动**。

**自动化方式（两条互补）：**

- **方式 A — 内嵌 web 服务**：`run_web.py` 启动时起一个后台定时线程，每天固定时间（如 23:00）自动调用 LTM 摘要并写回三 MD。
  依赖：web 服务**常驻运行**。服务开着 → 摘要自动发生，用户零操作。
- **方式 B — Windows 任务计划**：在 Windows 任务计划程序里注册每天定时任务，执行 `python scripts/run_memory_digest.py`。
  依赖：电脑开机、任务已注册。服务不常驻也能跑，最稳。

**方案 C（GitHub Actions cron）已排除**：session.db 本地存储、不上传，CI 拿不到对话数据，无法做摘要。

**实现要点（方式 A）：**
- 后台线程 `while True`：计算到下次触发时间的秒数 → sleep → 调 `ltm.digest_day()` → 更新下次触发时间。
- 与 web 服务生命周期绑定：服务退出即停；每天只跑一次（记录上次摘要日期，避免重复）。
- 摘要占用一次 LLM 调用（DeepSeek flash，成本可忽略），失败不阻塞服务。

---

## 六、LLM 接入与防幻觉机制

### 1. LiteLLM 统一模型网关  `【已实现】`

- 所有 LLM 调用（主 agent、管线各环、简历解析、每日摘要）统一走 LiteLLM；`config/litellm.yaml` 声明模型清单（flash + pro）。
- **当前实际使用**：全链路 `deepseek/deepseek-v4-flash`（`llm.py` `DEFAULT_MODEL`）；`deepseek-v4-pro` 已在 config 声明、暂未启用（可随时切换）。
- 说明：`config/litellm.yaml` 目前是文档性声明，代码里用 `litellm.completion(model="deepseek/deepseek-v4-flash")` 直接调用，未接 LiteLLM proxy server。
- **生成阶段统一控制**：低温（0.1）、低 top_p（0.9）、固定 seed（42）→ 输出稳定、可复现。
- ⚠️ **DeepSeek 推理模型必须禁 thinking**：deepseek-v4-flash 默认输出 `reasoning_content`（思考过程），而 ADK 2.6 的 LiteLlm 会**误把思考内容当回复**。所有调用必须带 `extra_body={"thinking": {"type": "disabled"}}`，否则回复是思考过程而非答案。这是已实测踩过的坑。
- ⚠️ **temperature/top_p 只影响生成采样，不影响检索**——"不给出不相关案例"由检索侧参数保证（top_k 小、相似度阈值），低温只是让生成不跑偏；低温 + 坏检索 = 自信地输出错误内容，必须配 grounding 约束。

### 2. 防幻觉机制（证据驱动）

| 机制 | 做法 | 对应组件 | 状态 |
|---|---|---|---|
| **工具调用防幻觉（第一道门）** | LLM 先判定消息类型再调工具：JD→`analyze_jd`、简历→`set_resume`、一般提问→禁止调工具直接回答；工具 docstring + INSTRUCTION 含"违反即为幻觉"硬规则（误判 / 绕过工具直接输出 / 拿不准时主动询问）；任何匹配度/硬门槛/source/needs_proof 只能来自工具返回报告，禁止自行编造 | agent + tools | `【已实现】` |
| **证据绑定** | 每个匹配分、每个差距点必须附证据引用（画像字段 / 案例 source）；无证据不写 | resume_matcher | `【已实现】` |
| **needs_proof 三态** | 技能/经历分三态：有证据 / 需补证据（`needs_proof`）/ 无证据；硬门槛无画像证据判 UNKNOWN | rules + resume_matcher | `【已实现】` |
| **硬门槛规则校验** | 学历、CPA、CET-6、实习年限、绩点等硬性要求用**确定性规则** + evidence_key 受控词表判定，不靠 LLM 自由判断 | analyzer/rules | `【已实现】` |
| **检索约束 + 阈值** | 只基于检索到的文档作答；检索为空明说"库中无匹配案例"。top_k 小（默认 3）、相似度阈值截断（默认 0.4，低于判不相关丢弃） | case_retriever | `【已实现】`（MVP 为纯向量检索，无 hybrid+rerank） |
| **bg 降级展示** | 岗位匹配到但案例库无上岸者 bg 时，如实标注"bg 知识库正在完善中"，不硬凑、不编造 | report | `【已实现】` |
| **结构化输出 + Schema 校验** | Pydantic schema 约束输出（JobRequirements / MatchReport / ReviewResult），校验失败抛错可重试 | 全部管线 | `【已实现】` |
| **reviewer 复核** | 复核 4 条硬规则：gap 无证据且非 needs_proof → error；有 fail 不得判"建议投递"；source 必须真实存在；strengths/suggestions 不超纲 | reviewer | `【已实现】` |
| **低置信度拒答** | 检索为空、证据缺失时明确标注，不硬答 | 全部 | `【已实现】` |
| **RAG 质量评测** | `scripts/eval_rag.py`：recall@k（对种子库案例构造查询看 top-k 是否召回自身） | eval_rag | `【已实现】` |
| **hybrid 检索 + rerank** | 语义 + 关键词混合检索、二路精排 | case_retriever | `【规划中】` |

---

## 七、Agent 架构与 Prompt 全景

> 本节厘清一个易混淆点：**工具不是纯代码，而是内部再调 LLM 的编排函数；但这不构成「多 agent」架构**——真 agent 只有主 agent 一个。下面是架构说明 + 全链路 LLM 调用点 + 每个 prompt 的全文。

### 1. 架构形态：单 Agent + 函数化管线（非多 agent）

**判断标准：有没有「决策循环」。**

| | Agent | 带 LLM 的函数 |
|---|---|---|
| 决策循环 | ✅ 有，自主决定下一步调什么、做几轮 | ❌ 无，一次性输入 → 输出 |
| 自主性 | 自己规划动作 | 顺序被调用方硬编码 |
| 多轮状态 | 有 | 无 |

- **真 agent 只有一个**：主 Agent `fin_resume`（`agents/fin_resume/agent.py`）——有决策循环：收消息 → LLM 判断是 JD 还是简历 → 决定调 `analyze_jd` 还是 `set_resume`（或 `search_web`）→ 拿到结果再决定怎么回。
- **内层「带 LLM」的环节都是函数，不是 agent**：`jd_analyzer` / `resume_matcher` / `reviewer` / `parser` 都是普通 Python 函数，内部做**一次** `complete_json`（输入 → JSON → 输出），无循环、不能自主决策；顺序被 `run_pipeline()` 写死（解析 → 校验 → 检索 → 匹配 → 复核）。

分层：

```
主 agent（1 个真 agent，有决策循环）
   └─ 工具函数（无决策循环）
        ├─ analyze_jd → run_pipeline 编排
        │     ├─ 纯代码环节：硬门槛校验(rules)、向量检索(case_retriever + embedding)
        │     └─ LLM 环节：解析 / 差距分析 / 复核（各带一个 prompt，一次性 complete_json）
        ├─ set_resume → 纯 LLM 环节（RESUME_PROMPT）
        └─ search_web → 纯代码环节（Tavily 联网搜索，无 LLM prompt）
```

**三种「代码」对照**（为什么有的环节有 prompt、有的没有）：

| 类型 | 有没有 prompt | 干什么 | 本系统例子 |
|---|---|---|---|
| 纯代码（确定性） | ❌ 无 | if-else / 词表映射 / 数学 / HTTP 调用 | 硬门槛校验 `rules.py`、联网搜索 `search_web` |
| embedding 模型 | ❌ 无 | 文本 → 向量算相似度（非生成式 LLM） | 向量检索 `case_retriever.py` |
| 生成式 LLM | ✅ 有 | 语义理解 / 判断 / 生成 | 解析 / 差距分析 / 复核等 |

> 第四章分层架构图已注明「**非多子 agent，四环为函数化管线**」——这是刻意取舍，不是遗漏。若真要改成多 agent，`reviewer` 会是独立 Agent（有自己的决策循环，如"发现 error 自主打回重跑"），而现在是 pipeline 里被顺序调一次的函数。

### 2. 全链路 LLM 调用点总览

| # | 调用点 | prompt 常量 | 文件 | 触发时机 |
|---|---|---|---|---|
| 1 | 主 Agent（调度 + 开场 + 报告硬规则 + 记忆规则 + 联网搜索） | `INSTRUCTION` | agents/fin_resume/agent.py | 每次对话 |
| 2 | JD 解析 | `EXTRACT_PROMPT` | analyzer/jd_analyzer.py | analyze_jd 第①环 |
| 3 | 差距分析 + 修改建议 | `MATCH_PROMPT` | matcher/resume_matcher.py | analyze_jd 第③环 |
| 4 | 复核防幻觉 | `REVIEW_PROMPT` | reviewer/reviewer.py | analyze_jd 第④环 |
| 5 | 简历 → 画像 | `RESUME_PROMPT` | resume/parser.py | set_resume |
| 6 | 每日摘要 | `DIGEST_PROMPT` | memory/ltm.py | 手动 `run_memory_digest.py` |
| 7 | 一键修改简历 | `REVISION_PROMPT` | revision/apply_revision.py | apply_revision |

> 另有两处「传给 LLM 但非 system prompt」：`set_resume` / `analyze_jd` / `search_web` 的**工具 docstring**（作为 function description 供主 agent 判断何时调用）；以及记忆注入 `inject_memory` 拼进 system_instruction 的画像/STM/LTM 文本（每次调 LLM 前追加，见第五章）。

### 3. Prompt 全文

#### ① INSTRUCTION（主 Agent — agents/fin_resume/agent.py）

```text
你是「财经求职助手」，帮助财经类院校学生分析岗位匹配度、提供简历修改建议。

【开场规则 · 最重要，任何情况下都必须遵守】
- 先检查本会话的历史消息：**是否已经有一条"我输出的开场白"**（即以"你好，我是「财经求职助手」"开头的 assistant 回复）。
- 如果**已经有了开场白** → 不要重复，直接处理用户需求。
- 如果**还没有开场白**（例如会话刚建立、你还没介绍过自己）→ 你的这条回复必须**先把下面的 WELCOME 开场白原样完整输出**（一个字都不要改、不要总结、不要省略），然后才继续处理用户的需求。
- **开场白输出完毕后，用户紧接着发来的 JD 或简历，必须照常调用 analyze_jd / set_resume**——开场白不改变工具调用规则，禁止因刚开场就改用纯文字回答。

WELCOME 全文：
你好，我是你的「财经求职私人助手」👋

把简历或岗位 JD 贴给我，我会做硬门槛校验、案例库匹配、差距分析和简历修改建议。

【工具调用硬规则 · 违反即为幻觉，必须严格遵守】
- 用户消息含招聘帖特征（"任职要求""学历要求""岗位职责""招聘""JD"）→ 调用 analyze_jd。
- 用户消息是个人简历 / 自我介绍（"我的""本人""教育背景""实习经历"等自述）→ 调用 set_resume。
- 用户聊天提问时，先判断是否**需要最新/实时信息**：
  * 问公司近况 / 行业动态 / 校招进展 / 岗位薪资行情 / 招聘时间点 / 某公司或行业评价等**时效性内容** → 调用 search_web 联网搜索，基于结果回答（保留来源链接）。
  * 问求职方法论 / 技巧等**通用知识**（简历怎么写、面试怎么准备、行研和投行怎么选、某类岗位做什么等）→ 直接文字回答，**不要调工具**。
- 拿不准用户发的是简历还是 JD → **不调工具**，直接问用户"这是你的简历还是岗位 JD？请确认一下"。
- 禁止为了"看起来在干活"而调用工具；能直接回答的问题就不要调工具。

工作方式（根据用户消息内容判断调用哪个工具）：
1. 用户发来**简历**（含教育背景 / 实习经历 / 技能 / 证书 / 项目等自我介绍或简历全文）
   → 调用 set_resume 保存画像，然后把返回的确认信息完整转述给用户，并提示下一步可以发 JD。
2. 用户发来**岗位 JD**（含岗位名称、任职要求、学历要求、工作职责等）
   → 调用 analyze_jd 做完整分析，然后把返回的分析报告**逐字原样完整输出**给用户，
     保留其全部章节结构，不做任何重新排版（详见「要求」里的报告输出硬规则）。
3. 用户问求职方法论 / 技巧等通用知识（简历怎么写、面试怎么准备、行业对比等）→ 直接回答，不需要调工具。
4. 用户既有简历又有 JD → 先 set_resume 再 analyze_jd。
5. 用户问需要联网搜索的时效性问题（公司近况、行业/校招动态、薪资行情、招聘时间点等）
   → 调用 search_web，把搜索结果组织成简洁的中文回答，**保留来源链接**；搜不到就如实说明，绝不编造。

要求：
- 必须用中文回答。
- 报告里有 `source=` 的引用不能去掉；有"待证据/需证据"项要如实保留。
- 不要编造报告里没有的内容。
- 联网搜索结果里的信息必须带来源链接引用；不编造搜索结果里没有的事实；搜索失败/无结果时如实告知用户，不硬凑。
- **【报告输出硬规则】analyze_jd 返回的报告必须原样输出，章节结构与措辞不得改动**：
  * 保留原有章节：① 硬门槛校验、② 匹配岗位 & 上岸背景画像（含「匹配到的岗位」「可借鉴的上岸背景案例」「背景：」）、③ 证据化差距分析（编号列表 + 匹配度 + 亮点）、④ 复核结论、⑤ 简历修改建议（按优先级）；
  * 禁止把列表改成表格；禁止新增「综合匹配度」「核心结论」「上岸者画像」「关键差异」「我的建议」「小结」等报告中不存在的标题、评分或总结；禁止重写措辞或自行添加 emoji 符号；
  * 禁止省略任何条目：硬门槛结果、source 引用、needs_proof / 待证据 / 需证据项都要保留。
- **任何匹配度、硬门槛判定、source 引用、needs_proof 标记，只能来自 analyze_jd 返回的报告原文**；
  若工具未返回报告，绝对不要自行编造匹配分数、或输出"根据您的背景估计…"之类的结论。
- 除第一条消息的开场白外，不需要再额外寒暄，直接按上述流程推进。

【系统记忆使用规则】
- 若出现「[用户画像摘要]」：这是用户通过 set_resume 保存的简历画像，跨会话持久，
  含姓名/学校/专业/技能/证书/实习/项目/意向。用它回答"我的简历是什么""我的背景"
  这类问题——直接引用画像内容，**绝不要说"没有保存过你的简历"或"我不知道你的背景"**。
- 若出现「[系统短期记忆]」：那是最近 2 小时内的对话摘要，
  用它理解当前语境（比如用户刚才在讨论哪个岗位、想对比什么），不要逐字复述给用户。
- 若出现「[系统长期记忆]」：包含用户画像、助手设定、长期经历（来源 data/memory 三类 MD）。
  用它做个性化回答——记住用户的目标岗位、已知短板、偏好城市、既往投递，避免重复问已知信息。
- 记忆是辅助：只在「有助于回答当前问题」时用，不要为了展示记忆而提它；记不住/没有记忆就正常按当前消息处理。
- 若记忆与用户本次消息冲突，以用户本次消息为准（记忆可能过时）。
```

#### ② EXTRACT_PROMPT（JD 解析 — analyzer/jd_analyzer.py）

```text
你是一名资深的财经行业招聘分析师。解析下面给出的岗位 JD，抽取结构化的岗位需求，输出 JSON。

只输出一个 JSON 对象，schema：
{
  "role_category": "岗位归类，从这些取值：券商行研 / 券商投行 / 银行管培 / 四大审计 / 基金投研 / 保险精算 / 咨询顾问 / 互联网财务 / 国企财务 / 考公事业单位",
  "role_name": "岗位名称",
  "company": "公司名（没有则为 null）",
  "location": "工作城市（没有则为 null）",
  "summary": "JD 一句话中文摘要（30 字以内）",
  "requirements": [
    {
      "type": "hard 或 soft 或 bonus",
      "category": "学历 / 证书 / 技能 / 经验 / 语言 / 其他",
      "description": "该需求的中文描述",
      "evidence_key": "从下方受控词表里选能精确对应的一条",
      "verdict_rule": "硬门槛判定规则的自然语言，如『硕士及以上』『持有 CPA』；非硬门槛填 null"
    }
  ]
}

evidence_key 受控词表（规则校验阶段用它在用户画像里定位证据，只能从这里选）：
- 学历：degree>=硕士 / degree>=本科
- 证书：cert:cpa / cert:cfa / cert:acca / cert:cet6 / cert:银行从业资格 / cert:证券从业资格 / cert:基金从业资格
- 技能：skill:python / skill:sql / skill:excel / skill:vba / skill:stata / skill:spss
- 实习：internships>=1 / internships>=3
- 绩点：gpa>=3.5 / gpa>=4.0
若 JD 里的要求在词表里没有精确对应项，选最接近的一条；实在对不上选一张描述最贴切的，并把规则写清进 description。

规则：
- 严格区分「岗位职责」与「任职要求」：
  * 「岗位职责」（"参与/协助/负责/研究/分析"等动词开头的段落）描述的是入职后的工作内容，
    不是对候选人的准入要求，不要把它们提取为 requirements。
  * 只有「任职要求/任职资格/我们希望你/职位要求」等段落里的内容才是要求条件。
  * 职责里提到的行业/赛道/产品方向（如"半导体""量子计算"）≠ 候选人必须具备该领域经验，
    不要据此生成"缺XX行业经验"的 gap。
  * 若 JD 没有明确分段，按语义判断：描述"做什么"的是职责，描述"要什么"的是要求。
- hard = 硬门槛（不满足不该投）；soft = 软性素质（可权衡）；bonus = 优先加分项。
- 只抽取 JD 里明确写出的要求，不臆造、不补常识性内容。
- 归并规则（重要）：同类条款合成一条，不要逐句拆散——
  * 时间/出勤类（到岗时间、实习时长、每周天数、实习周期、全勤与否等）→ 合并为一条，type=soft，category=时间出勤
  * 性格/素质类（自驱力、责任感、好奇心、抗压能力、沟通表达、团队协作等）→ 合并为一条，type=soft，category=软性素质
  * 学历、证书、技能、经验等实质性要求各自独立，不要归并
- 归并后的 description 用分号把各项串起来，如"尽快到岗；实习不少于3个月；每周至少4天"，不要写成多条。
- 只输出 JSON，不要任何多余文字。
```

#### ③ MATCH_PROMPT（差距分析 + 修改建议 — matcher/resume_matcher.py）

```text
你是一名深耕财经求职领域多年的专业顾问，对券商投行、行研、PE/VC、基金资管、银行、四大审计等各细分赛道的工作内容、技能栈、行业术语和简历惯例有深入理解。根据岗位需求、候选人画像、硬门槛校验结果、检索到的高分案例，做证据化的差距分析。只输出一个 JSON 对象：

{
  "match_score": 0到100整数，综合匹配度,
  "verdict": "建议投递 / 谨慎投递 / 不建议投递 之一",
  "gaps": [
    {
      "requirement": "需求描述（原样引用）",
      "status": "satisfied / gap / unknown 之一",
      "gap_description": "差距说明",
      "evidence": "判定依据：画像里的字段/证书/技能，或案例 source；没有就空串",
      "needs_proof": true或false,
      "suggestion": "具体可操作的简历修改建议"
    }
  ],
  "strengths": ["画像相对该岗位的亮点，逐条"],
  "top_suggestions": ["按优先级排序的简历修改建议，3到5条，每条2到4句话"],
  "sources": ["分析中实际引用过的案例 source，按如下格式：role_name @company (source=xxx)"]
}

规则（防幻觉，务必遵守）：
- 每条差距必须有 evidence 依据；画像和案例里都找不到依据的，needs_proof 必须为 true，
  并在 suggestion 里写明需要补充什么证据。
- 硬门槛校验里 fail 的项必须反映在 gaps 和 verdict 里；unknown 的项 needs_proof=true。
- 只基于给定的输入分析，不要脑补画像里没有的信息。
- 不要凭空编造案例内容，引用案例必须用其 source。
- 分析差距（gaps）时只对标 JD 的「任职要求」段；「岗位职责」段里提到的行业/方向
  （如半导体、量子计算）描述的是入职后的学习范围，不是候选人当前必须掌握的领域，
  不要据此判定"不匹配"。
- top_suggestions 必须体现财经行业专业性，不能浮于表面（这是核心要求，务必遵守）：
  * 结合岗位所属赛道（role_category）的行业知识与岗位职责，用专业术语给出具体建议。
    例如：投行岗 → 结合 IPO/并购重组/尽职调查/财务建模（DCF、可比公司估值）/募集说明书/底稿等；
    行研岗 → 结合深度报告/盈利预测/估值模型/晨会/路演/数据底稿（Wind、Bloomberg）等；
    PE/VC/资管 → 结合尽调/估值建模/LBO/投后管理/退出机制/一二级市场联动等；
    银行/四大 → 结合信贷尽调/审计底稿/内控测试/科目分析等。
  * 参考检索到的高分案例原文的简历写法（成功上岸者的背景/实习描述/项目成果是最好的范本），
    吸收其中的措辞、结构、量化方式，但不要写"参考了XX案例"这类话，直接给具体建议即可。
  * 每条建议写清：改简历哪个字段/段落 → 用什么专业措辞或行业标准写法 → 对标岗位哪个具体要求的哪一点、为什么这样改。
  * 每条 3~5 句，不要只写一句方向性的话，一定要落到可操作的层面（给出建议的具体措辞或数据表述）。
  * 主动利用 JD「岗位职责」里的行业/赛道信息：若职责提到半导体/电子/量子计算/新能源等方向，
    建议候选人提前阅读该行业的深度报告、学习产业链知识、了解主要公司和竞争格局，
    甚至建议在简历里加一个"行业研究/追踪"栏目体现主动性——职责里的行业方向就是最好的提前准备指南。
- gaps 里每条 gap 的 suggestion 同样要结合行业术语，与 top_suggestions 的专业水准保持一致。
- 遇到时间出勤、软性素质这类已归并的条目，gaps 里也只出一项，不要内部再拆开。
  整体判断是否吻合（如"出勤要求总体吻合/不吻合"），suggestion 一并给出，不逐项列。
- 只输出 JSON，不要多余文字。
```

#### ④ REVIEW_PROMPT（复核防幻觉 — reviewer/reviewer.py）

```text
你是一名严格的招聘流程质检员，负责复核求职助手输出的匹配报告，防止幻觉与错误结论。只输出一个 JSON 对象：

{
  "approved": true或false,
  "verdict_sound": true或false,
  "findings": [
    {"item": "问题对应项", "severity": "error或warning或info", "issue": "问题描述"}
  ],
  "corrections": ["具体修正建议"]
}

复核规则（逐条执行，只报「真正违反」的情况）：
1. 对每条 gap：若 evidence 为空且 needs_proof 不为 true → error（无证据却下结论，幻觉）。
   evidence 为空但 needs_proof=true 是合规的，不要报。
2. verdict 一致性：硬门槛有 fail 项，但 verdict 是『建议投递』→ error。
   verdict 是『谨慎投递』或『不建议投递』则合规，不要报。
3. sources：先在给定检索结果 source 列表里核对，能找到 → 合规，不要报；找不到 → error（编造来源）。
4. strengths / top_suggestions：若引用了画像或案例中不存在的内容 → error；都找得到 → 合规，不要报。

严重度含义：
- error = 违反规则，必须修正，foundings 里要有且 approved=false；
- warning = 可疑但不违反硬规则；
- info = 备注。
通过检查的项目不要写进 findings。不要为了凑数把合规项标 error。
拿不准的给 info，绝不夸大。只输出 JSON，不要多余文字。
```

#### ⑤ RESUME_PROMPT（简历解析 — resume/parser.py）

```text
你是一名简历信息抽取器。从候选人简历文本中提取结构化信息，输出 JSON。

JSON schema（对应 profile.yaml 的 user 字段）：
{
  "name": "姓名",
  "school": "学校",
  "major": "专业",
  "degree": "本科/硕士/博士等",
  "gpa": "绩点，如 3.5/4.0；无则空串",
  "target_roles": ["意向岗位"],
  "target_city": ["意向城市"],
  "constraints": ["限制条件，如每周可实习天数"],
  "skills": [{"name": "Python", "level": "中/高/熟练等", "evidence": "证明来源：课程/项目/证书"}],
  "certs": ["CPA", "CET-6", "证券从业资格"],
  "internships": ["公司-岗位（时长，可加成果要点）"],
  "projects": [{"name": "项目名", "detail": "内容与量化结果"}]
}

规则：
- 简历里没有的信息留空字符串或空列表，绝不编造。
- skills 的 evidence 写清"从哪能证明"（课程/项目/证书），没有就空串。
- internships / projects 尽量保留可量化的成果。
- 只输出 JSON，不要多余文字。
```

#### ⑥ DIGEST_PROMPT（每日摘要 — memory/ltm.py）

```text
你是一名求职私人助理的记忆整理员。把用户今天与「财经求职助手」的对话整理成长期记忆，只输出一个 JSON 对象，三个字段：

{
  "user_profile_updates": ["画像类记忆，如：用户目标是XX方向、某证书是短板、投递了哪家公司、求职进展等"],
  "agent_setting_updates": ["行为偏好类记忆，如：用户希望回答更简洁、对'待证据'标记很在意、强调不要编造等"],
  "dream_updates": ["经历类记忆，如：今天分析了哪几份JD、做了哪些对比决策、发现了什么信息、用户表达了什么倾向"]
}

规则：
- 每个字段是字符串数组，每项一句话，宁缺毋滥；没有对应内容就空数组 []。
- 只基于对话中真实出现的信息，不要脑补。
- 对话中的敏感个人信息（姓名、联系方式）不要写入，写"用户"。
- 只输出 JSON，不要多余文字。
```

---

## 八、LLM 约束机制（Harness）

> 一句话：**不信任 LLM 的自由文本，把每个「不可靠点」外包给一层更可靠的机制兜底**。LLM 只负责语义理解 + 生成建议，关键判断（硬门槛、相关性、证据、格式）全部被代码、词表、二次复核框住。

### 1. 五层约束

**第 1 层：模型参数（生成前的最底层缰绳）**
- `temperature=0.1` + `top_p=0.9` + `seed=42` → 低温稳定、固定种子可复现
- `extra_body={"thinking":{"type":"disabled"}}` → 禁用推理链，防止 DeepSeek 的 `reasoning_content` 被 ADK 误当回复

**第 2 层：Prompt 硬规则（指令约束）**
- INSTRUCTION 四组硬规则：开场规则 / 工具调用硬规则 / 报告输出硬规则 / 记忆使用规则（全文见第七章）
- 工具 docstring：写明 `调用条件 / 不要调用`，LLM 靠它判断何时调用、何时别乱调
- 各环节 `*_PROMPT` 的「规则（防幻觉，务必遵守）」段

**第 3 层：结构化输出（把输出框进 schema）**
- `complete_json()`：`response_format=json_object` + 兜底剥离 ```json``` 与前后废话
- Pydantic BaseModel：`JobRequirements / Requirement / MatchReport / GapItem / ReviewResult`，`model_validate` 强校验
- 受控词表 `EVIDENCE_KEYS`（20 键）：`evidence_key` 只能从中选（详见 §3），下游代码才能精确匹配

**第 4 层：代码确定性校验（关键判断下放给代码，不信 LLM）**
- `rules.py` `validate_hard_gates`：硬门槛用纯代码 if-else（`resolve_evidence` 解析 degree/cert/skill/internships/gpa），输出 PASS / FAIL / UNKNOWN；画像无证据 → UNKNOWN（needs_proof），宁可标未知不默认通过
- `case_retriever`：embedding + 相似度阈值 `min_sim` + `top_k`，低于阈值丢弃（宁缺毋滥）+ 强制带 `source` 溯源

**第 5 层：二次复核（LLM 自纠 + 硬门拦截）**
- `reviewer.review()`：另一个 LLM 交叉检查 MatchReport，四条硬规则：
  1. 每条 gap 必须有 evidence，否则 needs_proof 必须为 true（否则 = 幻觉，报 error）
  2. verdict 与硬门槛一致（有 fail 却判「建议投递」→ error）
  3. 引用的 source 必须真实存在于检索结果里（否则 = 编造来源）
  4. strengths / suggestions 不得引用画像或案例里不存在的内容
- 有 error 级 finding → `approved=False` → 主链路拦截，报告带「⚠ 复核未通过」

### 2. 贯穿原则：证据化 / 溯源

所有结论必须可溯源：证据来自画像字段、受控词表或案例 `source`；查不到就标 `needs_proof`，**绝不编造**。检索宁缺毋滥、硬门槛宁标未知，都是这条原则的具体化。

### 3. 受控词表 EVIDENCE_KEYS 详解

受控词表是「JD 解析（LLM）」与「硬门槛校验（代码）」之间的**固定字典 / 接口协议**，逼 LLM 只做翻译、不做自由发挥。

```
JD 原文 ──①LLM 抽取──▶ evidence_key ──②代码校验──▶ 画像证据 ──▶ PASS/FAIL/UNKNOWN
```

① 是 LLM（EXTRACT_PROMPT），② 是纯代码（rules.py `resolve_evidence`），② 只能靠**字符串前缀**识别键类型：

```python
if evidence_key.startswith("degree>="):       # 学历比较
if evidence_key.startswith("cert:"):          # 证书匹配
if evidence_key.startswith("skill:"):         # 技能匹配
if evidence_key.startswith("internships>="):  # 实习段数
if evidence_key.startswith("gpa>="):          # 绩点比较
```

所以 ① 的输出必须落进这 5 种前缀，否则 ② 对不上。20 个合法键：

```
degree>=博士 / degree>=硕士 / degree>=本科                          # 学历 3
cert:cpa / cert:cfa / cert:acca / cert:cet6                       # 证书 7
cert:银行从业资格 / cert:证券从业资格 / cert:基金从业资格
skill:python / skill:sql / skill:excel / skill:vba / skill:stata / skill:spss  # 技能 6
internships>=1 / internships>=3                                   # 实习段数 2
gpa>=3.5 / gpa>=4.0                                               # 绩点 2
```

**端到端例子**：JD 写「硕士及以上；持有 CPA；熟练 Python」。
1. LLM 对「硕士及以上」输出 `evidence_key = "degree>=硕士"`（而不是自由写"学历要硕士"）。
2. `resolve_evidence(profile, "degree>=硕士")` 看到 `degree>=` 前缀，去画像 `user.degree` 比对：`硕士`→PASS、`本科`→FAIL、没填→UNKNOWN。

**为什么必须受控**：若 LLM 自由发挥写 `evidence_key="需要硕士学历"`，5 个 `startswith` 全部落空 → 判 UNKNOWN，结果用户画像明明有学历证据却报「待证据」。受控词表把 LLM 的输出自由度收窄到固定词典，保证上下游严丝合缝；而且选错键最坏也只是落到 UNKNOWN（错得安全），不会误判 PASS/FAIL。

### 4. 约束的「外包」逻辑

| LLM 的不可靠点 | 外包给谁 |
|---|---|
| 判断「是否满足硬门槛」 | `rules.py` 纯代码 |
| 判断「检索是否相关」 | embedding + 阈值 |
| 选「evidence_key」 | 受控词表 |
| 输出「会不会编造」 | `reviewer` 二次复核 |
| 输出「格式」 | Pydantic + `json_object` |

严格说，LLM 在这里只是「语义抽取器 + 建议生成器」，判断权和格式权都不在它手里。这正是第七章「非多子 agent、四环为函数化管线」的深层原因：约束是分层塞进管线每一环的，而不是靠一个超级 prompt。

---

## 九、数据来源与策略（财经向）

### 可用的公开数据源

| 类型 | 来源 | 说明 |
|---|---|---|
| 校招 JD | 各券商/银行/四大/央企**官方校招页**、校招公众号、应届生求职网、牛客网 | 优先官方与开放源 |
| 简历案例 | 应届生论坛晒offer、知乎财经求职话题、牛客网、小红书 bg 帖 | 人工筛选过的公开内容，已脱敏 |
| 招聘趋势 | 人行/金融监管招录公告、CPA/CFA 报考要求、公开薪资报告 | 辅助信息 |
| 笔试面试面经 | 牛客网题库、应届生论坛面经帖 | 可作增强内容 |

### 入库格式（实际 yaml 结构）

知识库是 `data/knowledge/` 下的两类 yaml，按 `role_category` 分子目录存放，手工精选、`source` 必填溯源：

**JD 条目**（`jds/<role_category>/*-JD-###.yaml`）：

```yaml
jd_id: 国信证券债承-JD-001
role_category: 券商投行
role_name: 债券承做暑期实习生（有留用名额）
company: 国信证券
source: 用户提供/投行JD-002
summary: 国信证券债券承做暑期实习，全流程参与债承与发行，有留用，北京金融街
jd_text: |
  【工作地点】北京金融街
  【工作内容】...
  【任职要求】2027年毕业；本、硕均为双一流或QS前100高校...
```

**背景画像案例**（`cases/<role_category>/*.yaml`）——素材形态是 bg 帖/公开面经，不是完整简历：

```yaml
case_id: 投行-001
role_category: 券商投行
role_name: 券商/银行校招（综合金融）
source: 小红书/bg帖-用户提供-001
bg: 财贸本+英硕，一段四大实习+四段券商实习，秋招 all in 金融...
result: 秋招主投银行和券商
resume:
  school: 财贸本科（两财一贸级） + 英国硕士
  degree: 硕士
  internships: ["四大会计师事务所 实习（1段）", "券商 实习（4段，覆盖前中后台）"]
  certs: []
  skills: []
keywords: [券商, 银行, 四大, 校招, 财贸, 英硕, 综合金融]
```

> `【规划中】`自动收集的条目带 `status: seed | candidate | approved` 进审核流，人工/规则核验后才进正式库。

---

## 十、落地路径

### MVP 已完成（当前状态）

1. **种子知识库**：jds **16 条**（10 类岗位）、cases **3 条**（券商投行/行研）。⚠️ 数据收集是最大时间黑洞，用真实 JD / bg 帖整理，**不用 LLM 编造充数**（项目卖点是证据驱动）。
2. **记忆层**：`profile.yaml` 画像 + `history.db` 投递历史 + STM（极简）+ LTM（每日摘要三 MD）+ `before_model_callback` 注入。
3. **核心闭环**：输入 JD → `jd_analyzer` 抽需求（硬门槛规则校验）→ `case_retriever` 检索匹配岗位 + 上岸者背景画像 → `resume_matcher` 证据化差距分析（needs_proof）→ `reviewer` 复核。
4. **防幻觉优先**：reviewer + 证据绑定 + needs_proof + bg 降级展示 + RAG 评测（`scripts/eval_rag.py`）提前落地。
5. **接入**：LiteLLM（DeepSeek flash）+ `adk web` 可视化（`run_web.py`，新会话自动开场白 + 文件上传解码）。
6. **验收演示**：输入真实 JD → 输出 ① 硬门槛校验 ② 匹配岗位+案例（带 source）③ 证据化差距分析（含 needs_proof）④ reviewer 复核 ⑤ 简历修改建议；发简历后画像跨会话保留。

### 规划中（按优先级）

- **角色画像扩展**：补充更多 bg 案例（券商投行 / 行研外的岗位类别），`scripts/` 冒烟回归。
- **增强 1：联网搜索**：接入 web_search 工具（Tavily / Serper / Google CSE），对话中实时补充搜索；补齐 `role_profiler`（仅给岗位名也能分析）。
- **增强 2：定时自动收集**：GitHub Actions cron 每日触发 collector → 抓取 → 过滤 → 入库（查重投递历史）；自动抓取的进 `candidates` 表，审核通过后才进正式库。
- **增强 3：工程化收尾**：RAG hybrid + rerank；README 架构图 + demo 截图；RAG 评测扩展（证据覆盖率、幻觉率）。
- **增强 4：一键修改（apply_revision）**：逐条确认后应用修改、写回画像与投递历史。

---

## 十一、简历价值（面试讲点）

- **垂直场景差异化**：聚焦财经类院校就业，垂直知识库比通用"岗位匹配"更有护城河，且作者本人即目标用户。
- **私人助理式记忆**：用户画像 + 投递历史 + STM/LTM 四层记忆、`before_model_callback` 每次注入，跨会话连续性可演示。
- **RAG + 记忆层**：按财经岗位分 collection 的向量检索 + 记忆，检索与记忆质量均可量化。
- **防幻觉机制**：证据绑定、needs_proof、硬门槛规则校验、reviewer 复核、RAG 评测——AI 工程面试最加分的设计点。
- **四环管线**：JD 解析 / 硬门槛 / 检索 / 匹配 / 复核分工清晰，体现工程化（函数化管线 + Pydantic schema 契约）。
- **统一模型接入**：LiteLLM 网关，多模型可切换、可回退，体现工程完备性。
- **真实可用**：作者本人求职季使用，README 放真实跑通 demo。
- **定时数据管道**：`【规划中】`GitHub Actions cron + 增量更新，CI/CD 一体。

---

## 十二、风险与注意

- **数据质量是最大成本**：财经岗位的高质量简历案例集中在求职社区/公众号，可自动抓的有限 → 用种子库保底 + 人工策展。
- **合法性**：不硬爬 BOSS直聘 / LinkedIn 等 ToS 禁止的平台；校招信息优先走官方校招页。
- **隐私**：用户画像、对话摘要含真实个人信息，默认 `.gitignore` 排除（`.env`、`data/profile.yaml`、`data/knowledge/`、`data/memory/`、`data/chroma/`、`data/history.db`）、本地存储、不上传。
- **防幻觉是持续工程**：防幻觉机制要配 RAG 评测一起落地，否则难以证明有效。
- **范围控制**：全量功能很大，务必按 MVP → 增强分阶段推进，岗位范围先做深再扩展。
